<?php
    /*Imprimir por pantalla con echo.*/
echo "Hola usando echo.<br>";
echo "Este es un argumento.<br>", "Este es otro argumento.<br>";

    /*Imprimir por pantalla con print.*/
print "Hola usando print";

/*
        NOTAS  SOBRE (print):
    · (print) es una función, por ende es más lenta q (echo).

    · (print) solo puede devolver un argumento, (echo) puede devolver uno o varios argumentos a la vez.

    · (print) devuelve el valor de uno (1) si se lo asigna a una variable, (se verá mas adelante).

    · No utilizar (print) a menos que sea estrictamente necesario.
*/

/*
        NOTAS SOBRE SALTO DE LÍNEA:
    · Si se ejecuta el codigo en el navegador, hay q utilizar <br> para generar un salto de línea.

    · Si se ejecuta cen (la terminal / línea de comandos / consola), hay q utilizar (\n) para generar un salto de línea.
*/
?>